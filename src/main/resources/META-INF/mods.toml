modLoader="javafml" #mandatory
loaderVersion="[40,)"
license="GNU GPLv3"
issueTrackerURL="https://github.com/thethirdswan/tfc-food-process-compat/issues"
#clientSideOnly=true #optional - defaults to false if absent
[[mods]]
modId="tfc_food_compat"
version="1.0.0" #mandatory
displayName="TFC Food Processing Compat"
# A URL to query for updates for this mod. See the JSON update specification https://docs.minecraftforge.net/en/latest/misc/updatechecker/
#updateJSONURL="https://change.me.example.invalid/updates.json" #optional
# A URL for the "homepage" for this mod, displayed in the mod UI
displayURL="https://github.com/thethirdswan/tfc-food-process-compat"
# A file name (in the root of the mod JAR) containing a logo for display
#logoFile="examplemod.png" #optional, TBA
credits="simibubi, vectorwing, EERussianguy"
# A text field displayed in the mod UI
authors="The Third Swan" #optional
# Display Test controls the display for your mod in the server connection screen
# MATCH_VERSION means that your mod will cause a red X if the versions on client and server differ. This is the default behaviour and should be what you choose if you have server and client elements to your mod.
# IGNORE_SERVER_VERSION means that your mod will not cause a red X if it's present on the server but not on the client. This is what you should use if you're a server only mod.
# IGNORE_ALL_VERSION means that your mod will not cause a red X if it's present on the client or the server. This is a special case and should only be used if your mod has no server component.
# NONE means that no display test is set on your mod. You need to do this yourself, see IExtensionPoint.DisplayTest for more information. You can define any scheme you wish with this value.
# IMPORTANT NOTE: this is NOT an instruction as to which environments (CLIENT or DEDICATED SERVER) your mod loads on. Your mod should load (and maybe do nothing!) whereever it finds itself.
#displayTest="MATCH_VERSION" # if nothing is specified, MATCH_VERSION is the default when clientSideOnly=false, otherwise IGNORE_ALL_VERSION when clientSideOnly=true (#optional)

description='''
Enables TFC food data preservation when processing TFC foods with other mods!
'''
[[dependencies.tfc_food_compat]]
modId="forge"
# Does this dependency have to exist - if not, ordering below must be specified
mandatory=true
versionRange="[40,)"
# An ordering relationship for the dependency - BEFORE or AFTER required if the dependency is not mandatory
# BEFORE - This mod is loaded BEFORE the dependency
# AFTER - This mod is loaded AFTER the dependency
ordering="NONE"
side="BOTH"
[[dependencies.tfc_food_compat]]
modId="minecraft"
mandatory=true
versionRange="[1.18.2,)"
ordering="NONE"
side="BOTH"
[[dependencies.tfc_food_compat]]
modId = "tfc"
mandatory = true
versionRange = "[2.2.33,)"
ordering = "NONE"
side = "CLIENT"
[[dependencies.tfc_food_compat]]
modId = "patchouli"
mandatory = true
versionRange = "[1.18.2-67,)"
ordering = "NONE"
side = "CLIENT"
[[dependencies.tfc_food_compat]]
modId = "firmalife"
mandatory = false
versionRange = "[1.2.18,)"
ordering = "AFTER"
side = "CLIENT"
[[dependencies.tfc_food_compat]]
modId = "farmersdelight"
mandatory = false
versionRange = "[1.2.3,)"
ordering = "AFTER"
side = "CLIENT"
[[dependencies.tfc_food_compat]]
modId = "create"
mandatory = false
versionRange = "[0.5.1.e,)"
ordering = "BEFORE"
side = "BOTH"